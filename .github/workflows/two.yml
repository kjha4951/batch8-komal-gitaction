name: workflow1

on:
  push:
    branches: [ "main" ]

  #  workflow_dispatch:

jobs:
  jobfortwo:
    runs-on: self-hosted

    steps:
    
    
    
    
    - uses: actions/checkout@v3
      
    - name: Run a one-line script
      run: echo this is from vs and file name is two.yml
